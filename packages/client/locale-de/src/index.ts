/**
 * German language pack, node half. The empty apply keeps the browser feature
 * addressable from the host-owned Loader overlay.
 */

/** Host plugin body — the German dictionaries exist only in the browser entry. */
export function apply(): void {}
